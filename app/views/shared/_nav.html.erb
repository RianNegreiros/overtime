<div class='logo'>
  <h1>Time Tracker</h1>
</div>

<hr>

<% if user_signed_in? %>
  <div class="nav-tabs">
    <ul class="custom-nav nav nav-tabs">
      <li class="<%= active?(root_path) %>"><%= link_to "", root_path, class: "glyphicon glyphicon-home" %></li>

      <%= render 'shared/employee_nav_items' if employee? %>

      <%= render 'shared/audit_log_nav' if admin? %>

    </ul>
    <ul class="custom-nav nav nav-tabs pull-right">
      <% if current_user.type == 'AdminUser' %>
        <li><%= link_to "Admin Dashboard", admin_root_path %></li>
      <% end %>
      <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
    </ul>
  </div>
<% end %>